import { WebCharactersDirective } from './web-characters.directive';

describe('WebCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new WebCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
