import { PhoneCharactersDirective } from './phone-characters.directive';

describe('PhoneCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
