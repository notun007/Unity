import { FaxCharactersDirective } from './fax-characters.directive';

describe('FaxCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new FaxCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
